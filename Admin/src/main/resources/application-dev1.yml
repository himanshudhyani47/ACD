server:
  port: 9001

eureka:
  instance:
    preferIpAddress: true
    host: localhost
    port: 7001
  client:
    serviceUrl:
      defaultZone: http://${eureka.instance.host}:${eureka.instance.port}/eureka/